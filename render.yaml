services:
  - type: web
    name: 1fi-assignment
    runtime: node
    plan: free
    buildCommand: npm install && npm run db:deploy && npm run db:seed && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:./dev.db
